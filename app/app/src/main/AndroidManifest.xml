<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:label="Covelight"
        android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen">

        <!--
          T2.1 (docs/plan/03-phase2-kiosk.md): fullscreen immersive, no
          Device Owner / lock-task yet (T2.2). screenOrientation="portrait"
          mirrors /shell's own locked orientation
          (display/window/handheld/orientation in shell/project.godot,
          docs/design/activity-sdk.md #9) so the Android window never
          disagrees with what the engine already assumes. configChanges
          disables the automatic resize/recreate Godot's own Android docs
          warn is unsupported and can crash
          (docs.godotengine.org/en/stable/tutorials/platform/android/android_library.html).
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboardHidden|uiMode"
            android:resizeableActivity="false">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
