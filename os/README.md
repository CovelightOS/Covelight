# /os

Tier 2: Covelight OS image build. pmbootstrap configs, kernel configs (radio
drivers and firmware excluded — the structural air gap), cage Wayland kiosk
autostart, dm-verity read-only root, three-user privilege separation
(`kid`/`sync`/`root`).

**Tier:** tier2

**Status:** not started — see `docs/plan/04-phase3-to-6-skeletons.md`
(Phase 4, QEMU aarch64 image; Phase 5 for real hardware).
